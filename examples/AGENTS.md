# Example Instructions

Examples must run from a clean checkout without credentials when they use
deterministic mode. Keep them short, copy-pastable, and based on public engine
interfaces rather than product-specific helpers.

Run each changed example directly with `uv run python` before handoff.
