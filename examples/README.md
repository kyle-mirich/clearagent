# Examples

- `summarizer.py` — build an agent from a goal with the CLI-equivalent Python API.

Run the CLI instead:

```bash
uv run clearagent build "Build a release notes summarizer for changelog entries." --deterministic --export prompt.md
```
