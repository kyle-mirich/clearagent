# Documentation Instructions

Document the public engine as an installable local-first backend. Keep examples
and commands aligned with the current `src/clearagent` tree.

Do not describe Studio-only routes, hosted authentication, Qdrant ingestion, or
the private frontend as part of this repository. When the engine interface or
CLI changes, update the README and architecture map in the same change.
